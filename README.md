# Celestia

System that runs on servers to allow remote Docker control and monitoring from anywhere.

# Notes

Need to run **sudo chmod o+rw /var/run/docker.sock** before running the server for the first time (installation)
